name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    # container:
    # image: node:20
    steps:
      - uses: actions/checkout@v3
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v4
        # with:
        # init: none
        # planner: linux
      #- name: Run the Magic Nix Cache
      #  uses: DeterminateSystems/magic-nix-cache-action@main
      - run: nix build .#test || (ls -la /build/source; ls -la /build/source/target/release; ls -la /build/source/target/release/build; ls -la /build/source/target/release/build/libc-ff4c016e9ae90a0f; RUST_BACKTRACE=1 /build/source/target/release/build/libc-ff4c016e9ae90a0f/build-script-build) || (RUST_BACKTRACE=1 /build/source/target/release/build/target-lexicon-e285f28de2e78038/build-script-build) #; cat /build/source/target/release/build/target-lexicon-e285f28de2e78038/build-script-build)
      # - run: nix build .#wheel
      # - run: nix build .#docker
      # - run: nix build .#coverage_lcov
